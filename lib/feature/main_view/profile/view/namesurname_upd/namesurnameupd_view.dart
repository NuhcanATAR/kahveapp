import 'package:flutter/material.dart';

class NameSurnameUpdView extends StatelessWidget {
  const NameSurnameUpdView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Ad Soyad Güncelle"),
      ),
    );
  }
}
